server {
    listen <%= @port %>;

    server_name <%= @hostname %> localhost;

    root <%= scope.lookupvar('trivialaptrepo::params::reporoot') %>;

    location / {
        try_files $uri $uri/ =404;
        autoindex on;
    }
}
